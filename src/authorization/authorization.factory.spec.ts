import { AuthorizationFactory } from './authorization.factory';

describe('AuthorizationFactory', () => {
  it('should be defined', () => {
    expect(new AuthorizationFactory()).toBeDefined();
  });
});
